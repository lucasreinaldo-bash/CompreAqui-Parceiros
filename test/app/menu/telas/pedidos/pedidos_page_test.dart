import 'package:flutter/material.dart';
import 'package:flutter_test/flutter_test.dart';
import 'package:flutter_modular/flutter_modular_test.dart';

import 'package:parceiroscompreaqui/app/menu/telas/pedidos/pedidos_page.dart';

main() {
  testWidgets('PedidosPage has title', (WidgetTester tester) async {
    //  await tester.pumpWidget(buildTestableWidget(PedidosPage(title: 'Pedidos')));
    //  final titleFinder = find.text('Pedidos');
    //  expect(titleFinder, findsOneWidget);
  });
}
