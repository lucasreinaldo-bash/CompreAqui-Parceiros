import 'package:flutter_modular/flutter_modular_test.dart';
import 'package:flutter_test/flutter_test.dart';

import 'package:parceiroscompreaqui/app/menu/menu_controller.dart';
import 'package:parceiroscompreaqui/app/menu/menu_module.dart';

void main() {
  initModule(MenuModule());
  // MenuController menu;
  //
  setUp(() {
    //     menu = MenuModule.to.get<MenuController>();
  });

  group('MenuController Test', () {
    //   test("First Test", () {
    //     expect(menu, isInstanceOf<MenuController>());
    //   });

    //   test("Set Value", () {
    //     expect(menu.value, equals(0));
    //     menu.increment();
    //     expect(menu.value, equals(1));
    //   });
  });
}
