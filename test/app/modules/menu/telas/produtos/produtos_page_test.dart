import 'package:flutter/material.dart';
import 'package:flutter_test/flutter_test.dart';
import 'package:flutter_modular/flutter_modular_test.dart';

import 'package:parceiroscompreaqui/app/modules/menu/telas/produtos/produtos_page.dart';

main() {
  testWidgets('ProdutosPage has title', (WidgetTester tester) async {
    //  await tester.pumpWidget(buildTestableWidget(ProdutosPage(title: 'Produtos')));
    //  final titleFinder = find.text('Produtos');
    //  expect(titleFinder, findsOneWidget);
  });
}
